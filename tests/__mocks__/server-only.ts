// vitest stub for the `server-only` package. Next.js replaces this module with
// an empty module during server builds and a throwing module for client builds;
// under vitest we treat it as a no-op so server-only modules can be imported.
export {};
