## 1. Project content entries

- [x] 1.1 Add `discord-bot-framework` project entry to `src/content/projects/projects.ts` with all required model fields populated: slug, title, short description, status (`active`), technologies (Hikari, Lightbulb, Lavalink, FastAPI, SQLAlchemy, PostgreSQL, Python), `featured: false`, order, problem (wanted a personal bot where any feature can be disabled without touching core), architecture (plugin-driven core with first-party plugins under `plugins/`, RBAC permissions, async SQLAlchemy persistence, optional FastAPI control panel, Lavalink music stack), implementation, challenges (plugin system design — enabling/disabling plugins per guild without touching core), tradeoffs (plugin isolation adds indirection and complexity vs. a monolithic bot, but the ability to disable any feature without touching core was worth it), and links (repository: github.com/leonardomurakami/discord-bot-framework, live: bot.murakams.com)
- [x] 1.2 Add `gamdle` project entry to `src/content/projects/projects.ts` with all required model fields populated: slug, title, short description, status (`maintenance`), technologies (Node.js, PostgreSQL, esbuild, Docker), `featured: false`, order, problem (wanted a Wordle-style daily game around probability/casino mechanics with deterministic daily outcomes and passwordless accounts), architecture (Node.js backend with esbuild-bundled browser frontend, Postgres for persistence, magic-link auth via email webhook, Docker Compose for deployment), implementation, challenges (choosing between one deterministic seed across all users vs. one seed per user per day — decided on per-user-per-day so the game is luck-based in a no-risk environment rather than a single correct path), tradeoffs (per-user seed means no shared "correct answer" to compare — traded the social angle for fairness and replayability; having to be a little lucky is fun in a no-risk environment), and links (repository: github.com/leonardomurakami/gamdle, live: gamdle.murakams.com)
- [x] 1.3 Add `graduation-ime-usp` project entry to `src/content/projects/projects.ts` with all required model fields populated: slug, title, short description, status (`active`), technologies (Docusaurus, React, Node.js, Docker, Nginx), `featured: true`, order, problem (wanted a centralized, browsable portfolio of academic coursework across the IME-USP CS degree rather than just a GitHub directory tree), architecture (Docusaurus static site rendering coursework organized by year and discipline, Docker container with Nginx for serving, deployed via the GitOps platform), implementation, challenges (the hard challenge is the university degree itself — the site is a living record of that ongoing work), tradeoffs (static site over a dynamic one — zero maintenance, but no search or interactive features), and links (repository: github.com/leonardomurakami/graduation-ime-usp, live: graduacao.murakams.com)
- [x] 1.4 Add `message-board` project entry to `src/content/projects/projects.ts` with all required model fields populated: slug, title, short description, status (`completed`), technologies (FastAPI, HTMX, SQLAlchemy, PostgreSQL, SQLite, Python, Docker), `featured: false`, order, problem (individual assignment for MAC0350 at IME-USP — build a multi-user forum with real backend and database), architecture (FastAPI backend with Jinja2 templates, HTMX for dynamic UI without a JS framework, SQLAlchemy ORM with SQLite for dev and PostgreSQL for prod, Docker for deployment), implementation, challenges (designing the data model and SQLAlchemy ORM layer for a multi-user forum), tradeoffs (most design decisions were driven by class requirements — the constraints shaped the stack), and links (repository: github.com/leonardomurakami/message-board, live: msgbrd.murakams.com)

## 2. Visual material

- [x] 2.1 Add visual material (architecture diagram or screenshot) for `discord-bot-framework` under `public/projects/discord-bot-framework/` and reference it from the project entry's `visualMaterial` array with descriptive alt text
- [x] 2.2 Add visual material (architecture diagram or screenshot) for `gamdle` under `public/projects/gamdle/` and reference it from the project entry's `visualMaterial` array with descriptive alt text
- [x] 2.3 Add visual material (screenshot or diagram) for `graduation-ime-usp` under `public/projects/graduation-ime-usp/` and reference it from the project entry's `visualMaterial` array with descriptive alt text
- [x] 2.4 Add visual material (architecture diagram or screenshot) for `message-board` under `public/projects/message-board/` and reference it from the project entry's `visualMaterial` array with descriptive alt text

## 3. Ordering and homepage preview

- [x] 3.1 Assign `order` values to the four new project entries so the projects index presents them in a sensible order relative to the existing `homelab-gitops-platform` (order 1)
- [x] 3.2 Verify that `graduation-ime-usp` (the only new `featured: true` entry) appears in the homepage selected projects preview alongside the existing featured project

## 4. Verification

- [x] 4.1 Run `pnpm typecheck` and confirm the new project entries pass Zod schema validation at build time
- [x] 4.2 Run `pnpm build` and confirm all four new `/projects/[slug]` pages are generated via `generateStaticParams`
- [x] 4.3 Run `pnpm lint` and confirm no new lint errors
- [x] 4.4 Verify each new project detail page renders all content model sections (problem, architecture, implementation, challenges, trade-offs, visual material, links) in server-rendered HTML without JS
- [x] 4.5 Verify the `/projects` index lists all five projects with title, short description, status, and technologies
- [x] 4.6 Verify the homepage preview includes the featured `graduation-ime-usp` entry
